apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "code_server_k8s.fullname" . }}-test-connection"
  labels:
{{ include "code_server_k8s.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "code_server_k8s.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
